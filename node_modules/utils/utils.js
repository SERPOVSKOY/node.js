const connectToDB = () => {
    console.log('Connect OK');
}

const response = {
    status: 200,
    data: { text: 'My massage'}
}

module.exports = {
    connectToDB: connectToDB,
    response: response
}